@page
@model Admin.Pages.Competencies.Knowledge.IndexModel
@{
    ViewData["Title"] = "Competencies";
}

<h1>Competencies</h1>

<p>
    <a class="btn btn-dark" asp-page="/Competencies/Create">Create New</a>
</p>

<form asp-page="./Index" method="get">
    <div class="form-actions no-color">
        <h6>
            Find Competency:
            <input type="text" name="SearchString" value="@Model.CurrentFilter" />
            <input type="submit" value="Search" class="btn btn-primary" /> |
            <a asp-page="./Index">Back to Full List</a>
        </h6>
    </div>
</form>

<hr />

<table class="table">
    <thead>
        <tr>
            <th><b>Competency English</b></th>
            <th><b>Competency French</b></th>
            <th><b>Description English</b></th>
            <th><b>Description French</b></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Competency)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NameEng)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NameFre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DescEng)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DescFre)
                </td>
                <td>
                    <a asp-page="/Competencies/Create" asp-route-id="@item.Id">Create</a>
                    <a asp-page="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-page="Details" asp-route-id="@item.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
